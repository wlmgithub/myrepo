#!/bin/bash

### should match those in the presentation layer
PATH_TO_FILE='/Library/WebServer/Documents/dashboard/data/services_not_running/data.txt'
PATH_TO_FILE_TEMP='/Library/WebServer/Documents/dashboard/data/services_not_running/data.txt.tmp'


### DO NOT CHANGE THE FOLLOWING
echo "working ..."
#push_auditor -f prod-ech3 | awk '{print $1, $2, $5}' | grep -v 'running'  | sort -u   >  $PATH_TO_FILE
push_auditor -f prod-ech3 | grep -v 'running'  | sort -u   >  $PATH_TO_FILE_TEMP

chmod 0777 $PATH_TO_FILE_TEMP
chmod 0777 $PATH_TO_FILE

mv $PATH_TO_FILE_TEMP $PATH_TO_FILE
